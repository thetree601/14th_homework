적용된 메모이제이션
1. secrets-list/index.tsx (메인 리스트 페이지)
handleLoginClick → useCallback 적용
handleLogoutClick → useCallback 적용

2. hot-secrets/index.tsx
formatPrice → useCallback 적용
goToSlide → useCallback 적용
컴포넌트 → React.memo로 감싸서 props가 변경되지 않으면 리렌더링 방지

3. sale-secrets/index.tsx
formatPrice → useCallback 적용
handleSubscribe → useCallback 적용 (subscribe 의존성 포함)
컴포넌트 → React.memo로 감싸기

4. recommended-secrets/index.tsx
formatPrice → useCallback 적용
컴포넌트 → React.memo로 감싸기

5. my-secrets/index.tsx (마이페이지)
formatPrice → useCallback 적용
성능 개선 효과
함수 메모이제이션: 매 렌더링마다 함수가 재생성되지 않아 불필요한 리렌더링 감소
컴포넌트 메모이제이션: HotSecrets, SaleSecrets, RecommendedSecrets가 props가 동일하면 리렌더링되지 않음
부모 컴포넌트 리렌더링 시 하위 컴포넌트의 불필요한 리렌더링 방지
모든 변경사항이 적용되었고 린터 오류는 없습니다.